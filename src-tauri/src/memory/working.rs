/// Working memory — recent days, stored in MEMORY.md.
/// This is the bridge between ephemeral session state and long-term patterns.

// TODO (Session 2): Implement MEMORY.md journal.
// - Append significant events after each reasoning cycle.
// - Read at the start of every cycle for cross-session context.
// - Decay entries older than 30 days (move to long-term if pattern detected).
